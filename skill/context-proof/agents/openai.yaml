name: context-proof
version: 0.2.1
triggers:
  default_prompt: "Use $context-proof to audit this repository's AGENTS.md, CLAUDE.md, SKILL.md, and related agent context."
commands:
  audit: "python scripts/contextproof.py audit . --pr-comment"
  quickstart: "python scripts/contextproof.py quickstart ."
