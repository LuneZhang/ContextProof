# Project Brief From /init

This is a new TypeScript service. It should be production-ready, clean code,
and high quality. The agent should use your judgment and be careful.

The project will eventually include API routes, database migrations, background
jobs, and generated SDKs. Read the entire repository before every task because
the structure may change often.

Expected validation will probably include `npm test`, but the test setup is not
complete yet.
