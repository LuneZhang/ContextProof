# Existing Project: Overbroad Context

This scenario represents an existing repository with a hand-written `AGENTS.md`
that tells the coding agent to inspect too much, follow vague quality rules,
and run broad validation regardless of task size.

The optimizer should preserve the concrete validation command while replacing
global exploration mandates with task-scoped discovery.
