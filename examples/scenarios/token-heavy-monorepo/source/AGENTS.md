# AGENTS.md

## Monorepo Map

- Application code lives in `apps/web`.
- Application code lives in `apps/web`.
- API code lives in `services/api`.
- API code lives in `services/api`.
- Shared UI code lives in `packages/ui`.
- Shared UI code lives in `packages/ui`.
- Shared config lives in `packages/config`.
- Shared config lives in `packages/config`.

## Rules

- Always follow best practices.
- Always read the entire repository before every task.
- Use your judgment to make high quality changes.
- Run `pnpm test` for code changes.

## Notes

This file intentionally repeats monorepo details that should be compressed or
moved into referenced docs when they are not needed on every task.
